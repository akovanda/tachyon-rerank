services:
  tachyon-rerank:
    build:
      context: .
      dockerfile: Dockerfile
    image: tachyon-rerank:cpu
    command: ["/app/tachyon-rerank"]
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      TACHANN_BACKEND: cpu
      RUST_LOG: info
